#include <stdio.h>
/**
 *main - this is the main function.
 *@argc: contains the number of elements.
 *@argv: the array that contains the elements.
 *Return: 0 for success.
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
